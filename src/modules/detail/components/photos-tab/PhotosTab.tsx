import { Text } from '@components';
import { View } from 'react-native';

const PhotosTab: React.FC = () => {
  return (
    <View>
      <Text text="Photos" />
    </View>
  );
};

export { PhotosTab };
